Lo que hemos hecho es crear una conexión con nuestra base de datos. Despues de esto lo que hemos hecho es
crear un menu el cual lo que hace es conectar ala base de datos a el esquema que queremos y asi como lo conectamos
también lo que hemos hecho es el poder desconectarlo y luego dar la opción de crear el SQL junto con un
translaccionlog para ver los sql que hemos hecho. Para esto lo que hemos hecho hablando de codigo es lo siguiente como
hemos dicho crear los botones con el JButton y luego para los esquemas hemos utilizado el JCombobox. Despues
de esto de poder escoger la opción que queramos es el momento de abrir la base de datos si esta bien perfecto si no lanzara un error
si se le da disconect se hace claseDB si no funciona lanza un error. Tras esto si le hemos dado a SQL query habre
otro JOption pone en el que podemos escribir y si esta bien escrito nos dará un resultado y en transaction lo 
podemos ver en runqueries para el SQL lo que hemos hecho es un panel en el que podemos escribir y al darle al boton run query lo hace 
o tambien le podemos dar al boton back y va para atras luego tambien se ve los resultados. Para el TransactionLogs lo que hacemos
es el panel y que se crea un Jlabel vacio pero si hemos hecho alguna query aparece hay y tambien como el anterior podemos ir para atras y 
si no hay nada te pone error loading logs ya que no hemos hecho nada.